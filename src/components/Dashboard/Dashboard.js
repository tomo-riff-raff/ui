import React, { useContext } from "react";
import Pairings from '../Pairings/Pairings';

const Dashboard = () => {
  return (
    <header>
      <h1>Dashboard</h1>
      <Pairings />
    </header>
  )
}

export default Dashboard
